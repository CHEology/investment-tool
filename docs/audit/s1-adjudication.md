# S1 reconciliation adjudication (2026-08-28)

Dispositions of the review issues; full reasoning in the session transcript.

| # | Issue | Disposition |
|---|---|---|
| 1 | Replay validity | ACCEPTED. Temporal attribution invalid (H1 report first available 2026-08-29 Beijing > t0 2026-08-27; periodic report is not a causal negative on title match). Candidate reset to PENDING_ATTRIBUTION; card_09f757..._v1 marked INVALIDATED (kept for audit, excluded from control cohort); revenue proxy (2025H1+2026H1) recorded INVALID; economic classification left UNRESOLVED — no value re-derived toward any outcome. Mechanism components that did pass: trigger math, limit/PARTIAL handling, gates, fetch lineage, damage arithmetic (golden-tested), freeze/render. Unverified: valid attribution on real data; valid economic classification; EXCESS path end-to-end. |
| 2 | Price basis | ACCEPTED, raw-only REJECTED (送转/配股 ex-dates would fabricate Lane A triggers). Design: returns as analytical primitive — QFQ_CONSEC history (Tencent), EXCHANGE_PCT daily (snapshot; exchange pct is computed vs adjusted prev close), SYNTH_COMPOUND continuation, RAW_CONSEC only for BSE (Sina) with documented corporate-action risk. Per-row ret_basis + per-listing basis_epoch; provider rewrite detection via overlap check -> epoch bump + full-history replace; mixed raw/adjusted bases or mixed epochs BLOCK analytics (listing excluded, counted in audit). Raw prices stay canonical where sourced; NULL where unknown. Mcap reconstruction uses same-basis adjusted-close scaling, never raw-vs-adjusted mixing. Tests added (mixing blocks; epoch bump; split cannot trigger). |
| 3 | Tushare | ACCEPTED. No adj_factor assumption at entry tier (2,000-point requirement acknowledged; no paid authority). Adapter deferred until a token exists; entitlements to be probed at runtime and degraded honestly. `invest credentials set` uses hidden interactive input + keyring (no shell args/ps/logs/manifests exposure). |
| 4 | Announcement semantics | ACCEPTED with 4-way relevance: HARD_NEGATIVE / CONTENT_REVIEW_REQUIRED / POSITIVE / NEUTRAL. Attribution gate = temporally eligible HARD_NEGATIVE, or temporally eligible CONTENT_REVIEW + operator attribution_confirmation (recorded in the params file). Timestamp semantics: published (CNInfo date-granular, midnight-Beijing heuristic -> ts_precision), first_seen, eligible_from = Beijing date of publication; first_seen < published recorded as ts_anomaly FIRST_SEEN_BEFORE_PUBLISHED, never silently passed. |
| 5 | Market model | Reviewer CORRECT: code already gates on peer-adjusted CAR only; mm CAR is post-trigger diagnostic. Documentation fixed; no threshold change; no v0.1 needed for this. |
| 6 | Multi-horizon | Assessment: 1-session and 3-5-session dislocations covered ([0,+3] windows, 5-session t0 lookback); 10-20-session slow drawdowns NOT covered by admission (unchanged, by authority boundary) — added telemetry-only shadow observations car10<=-15% / car20<=-20% (config v0.1 `telemetry:`; cannot admit). Overlapping-window multiple testing mitigated by 10-session episode cooldown (anchor trigger; tightens only). Known calibration note: 4-session CAR vs 3x daily sigma mixes horizons (sigma leg looser than nominal); recorded for forward calibration, thresholds untouched. |
| 7 | Resilience | Added: jittered exponential backoff, Retry-After honoring, content validation (non-JSON -> CONTENT_INVALID quality), circuit breaker (8 consecutive failures -> 120s cooldown, second trip -> safe abort; resume via --skip-existing), provider-health summary. Bulk-by-date: no free bulk history source exists without a Tushare token; per-listing one-time backfill stands; daily incremental = one snapshot sweep. No identity rotation or access-control circumvention. |
| 8 | Scope items | Damage assumptions rendered on cards: DONE. Freeze query fixed (touched-candidate set): DONE. Audit lineage now reflects actual providers/bases: DONE. Volume-unit fixture (616,243 lots -> 61,624,300 shares): DONE. industry_snapshot duplicate writes removed (PIT kept in market_snapshot): DONE. Control cohort restricted to VALID artifacts: DONE. 23:45 run / sector-dislocation monitor / GDELT / event-first / launchd: DEFERRED to later slices (no S1 correctness dependency). |

Config: v0.1 = v0 + telemetry keys only; admission thresholds byte-identical (changelog in file).
